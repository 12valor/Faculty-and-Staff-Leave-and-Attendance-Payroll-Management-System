import { CalendarCheck2, ClockAlert, PhilippinePeso, UsersRound } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Card, CardAction, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { AttendanceChart } from "@/features/dashboard/components/attendance-chart";
import { PayrollDeductionsTable, type DeductionRow } from "@/features/dashboard/components/payroll-deductions-table";

type DashboardData = { totalEmployees: number; facultyCount: number; staffCount: number; presentToday: number; attendanceRate: number; pendingLeave: number; deductionTotal: number; periodLabel: string; leaveStatuses: Array<{ label: string; value: number; variant: "secondary" | "outline" | "destructive" }>; trend: Array<{ day: string; present: number; absent: number }>; deductions: DeductionRow[]; today: string };
export function DashboardOverview({ data }: { data: DashboardData }) {
  const metrics = [
    { label: "Total employees", value: String(data.totalEmployees), detail: `${data.facultyCount} faculty · ${data.staffCount} staff`, icon: UsersRound },
    { label: "Present today", value: String(data.presentToday), detail: `${data.attendanceRate}% attendance rate`, icon: CalendarCheck2 },
    { label: "Pending leave", value: String(data.pendingLeave), detail: "Applications awaiting review", icon: ClockAlert },
    { label: "Current deductions", value: `₱${data.deductionTotal.toLocaleString(undefined, { minimumFractionDigits: 2 })}`, detail: "Recorded attendance deductions", icon: PhilippinePeso },
  ];
  return <section className="flex flex-col gap-6"><div className="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><h2 className="text-2xl font-semibold tracking-tight md:text-3xl">Institutional overview</h2><p className="mt-2 text-sm text-muted-foreground md:text-base">Live workforce activity for {data.today}.</p></div><Badge variant="outline">{data.periodLabel}</Badge></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{metrics.map((metric) => { const Icon = metric.icon; return <Card key={metric.label} className="shadow-sm"><CardHeader><CardDescription>{metric.label}</CardDescription><CardAction><div className="flex size-10 items-center justify-center rounded-xl bg-secondary text-secondary-foreground"><Icon aria-hidden="true" /></div></CardAction><CardTitle className="font-mono text-2xl font-semibold">{metric.value}</CardTitle></CardHeader><CardContent><p className="text-xs text-muted-foreground">{metric.detail}</p></CardContent></Card>; })}</div><div className="grid gap-6 xl:grid-cols-[minmax(0,1.65fr)_minmax(320px,0.75fr)]"><Card className="shadow-sm"><CardHeader><CardTitle>Attendance trend</CardTitle><CardDescription>Present and absent records over the last five days.</CardDescription></CardHeader><CardContent><AttendanceChart data={data.trend} /></CardContent></Card><Card className="shadow-sm"><CardHeader><CardTitle>Leave status</CardTitle><CardDescription>Applications currently in the database.</CardDescription></CardHeader><CardContent className="flex flex-col gap-5">{data.leaveStatuses.map((status) => <div key={status.label} className="flex items-center justify-between border-b pb-4 last:border-b-0 last:pb-0"><p className="text-sm font-medium">{status.label}</p><Badge variant={status.variant}>{status.value}</Badge></div>)}</CardContent></Card></div><Card className="shadow-sm"><CardHeader><CardTitle>Recent attendance deductions</CardTitle><CardDescription>Latest computed deductions from encoded attendance.</CardDescription></CardHeader><CardContent className="overflow-x-auto"><PayrollDeductionsTable records={data.deductions} /></CardContent></Card></section>;
}